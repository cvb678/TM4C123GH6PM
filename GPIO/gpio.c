/* Each GPIO module clock must be enabled before 
** the registers can be programmed (p.340)
** There must be a delay of 3 system clocks after 
** the GPIO module clock is enabled before
** any GPIO module registers are accessed.
*/


Initialization and Configuration p.657
